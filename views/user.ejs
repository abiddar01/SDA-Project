<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Toyota - Pure Duality</title>
  <link rel="icon" href="https://toyota-indus.com/wp-content/uploads/2022/12/Fav-Icon-32x32-1.jpg" sizes="32x32" />
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500&display=swap" rel="stylesheet" />
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body,
    html {
      height: 100%;
      overflow: hidden;
      font-family: 'Orbitron', sans-serif;
      background: #000;
      color: white;
    }
    .hero {
      position: relative;
      height: 100vh;
      background: url('Main3.jpg') center/cover no-repeat;
      filter: brightness(2.1); /* brightens image */
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.7);
      z-index: 1;
    }
    .top-right {
  position: absolute;
  top: 20px;
  right: 30px;
  z-index: 3;

  opacity: 0;
  transform: translateY(-20px);
  animation: slideFadeIn 1s ease forwards;
  animation-delay: 2s;
}

@keyframes slideFadeIn {
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.login-button {
  background: linear-gradient(45deg, #e30613, #ff4d4d);
  color: white;
  border: none;
  padding: 12px 28px;
  font-size: 1.1em;
  border-radius: 30px;
  cursor: pointer;
  box-shadow:
    0 0 5px #e30613,
    0 0 15px #ff4d4d,
    0 0 30px #ff9999;
  transition: all 0.4s ease;
  font-weight: 700;
  letter-spacing: 1px;
  text-transform: uppercase;
  filter: drop-shadow(0 0 5px #ff0000);
}
.login-button:hover {
  background: linear-gradient(45deg, #ff4d4d, #e30613);
  box-shadow:
    0 0 10px #ff0000,
    0 0 25px #ff6666,
    0 0 40px #ff9999;
  transform: scale(1.05);
  color: #fff;
  filter: drop-shadow(0 0 8px #ff3300);
}


    .content {
      position: relative;
      z-index: 2;
      text-align: center;
      animation: fadeIn 2s ease-in forwards;
      opacity: 0;
      animation-delay: 2s;
    }
    @keyframes fadeIn {
      to {
        opacity: 1;
      }
    }
    h1 {
      font-size: 4em;
      color: #fff;
      margin-bottom: 20px;
      text-shadow: 0 0 10px rgba(255, 255, 255, 0.5);
    }
    .glow-button {
      background: #e30613;
      border: none;
      padding: 15px 40px;
      font-size: 1.2em;
      color: white;
      border-radius: 30px;
      cursor: pointer;
      box-shadow: 0 0 10px #e30613, 0 0 20px #e30613, 0 0 30px #e30613;
      animation: pulse 2s infinite;
    }
    @keyframes pulse {
      0% {
        transform: scale(1);
      }
      50% {
        transform: scale(1.1);
      }
      100% {
        transform: scale(1);
      }
    }
    .glow-button:hover {
      background: #fff;
      color: #e30613;
      box-shadow: 0 0 20px white;
    }
  </style>
</head>
<body>
  <div class="top-right">
    <button class="login-button" onclick="location.href='login'">Login</button>
  </div>

  <div class="hero">
    <div class="overlay"></div>
    <div class="content">
      <h1>PURE DUALITY</h1>
      <button class="glow-button" onclick="location.href='products'">Enter Showroom</button>
    </div>
  </div>

</body>
</html>
