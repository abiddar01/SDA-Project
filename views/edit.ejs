<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update Item</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500&display=swap" rel="stylesheet">
    <style>
      body {
        font-family: 'Orbitron', sans-serif;
        background: url('/edit.jpg') center/cover no-repeat;
      }
      .navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 40px;
  background-color: rgba(0, 0, 0, 0.8);
  box-shadow: 0 0 10px #e30613;
  position: fixed;        /* changed from sticky */
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
}

    .navbar .nav-links {
      display: flex;
      gap: 25px;
      list-style: none;
    }
    .navbar .nav-links li a {
      color: #fff;
      text-decoration: none;
      font-weight: bold;
      font-size: 1em;
      padding: 10px 15px;
      border-radius: 20px;
      transition: all 0.3s ease;
    }
    .navbar .nav-links li a:hover {
      background-color: #e30613;
      box-shadow: 0 0 10px #e30613;
    }
      .glow-form {
        background: rgba(0, 0, 0, 0.8);
        backdrop-filter: blur(20px);
        box-shadow:
          0 0 10px rgba(255, 255, 255, 0.1),
          0 0 20px rgba(255, 255, 255, 0.1),
          0 0 30px rgba(255, 255, 255, 0.1);
      }
      .glow-input:focus {
        outline: none;
        box-shadow:
          0 0 8px #ffffff,
          0 0 12px #ffffff;
        border-color: #fff;
      }
      .glow-button {
        background: linear-gradient(45deg, #444, #000);
        box-shadow:
          0 0 10px #ffffff,
          0 0 20px #999,
          0 0 30px #666;
        transition: all 0.3s ease-in-out;
      }
      .glow-button:hover {
        transform: scale(1.05);
        box-shadow:
          0 0 15px #fff,
          0 0 30px #aaa,
          0 0 45px #888;
      }
      label {
        color: #fff;
      }
    </style>
  </head>
  <body class="bg-black min-h-screen pt-28">
    <header class="navbar">
      <div class="logo">
        <span class="font-['Pacifico'] text-6xl text-white mb-3 tracking-wide">Elegance Wheels</span>
      </div>
      <nav>
        <ul class="nav-links">
          <li><a href="/dashboard">Home</a></li>
          <li><a href="/dashboard/create">Create</a></li>
          <li><a href="/dashboard/read">Read</a></li>
          <li><a href="/dashboard/orders">Orders</a></li>
          <li><a href="/logout">Logout</a></li>
        </ul>
      </nav>
    </header>
  
    <!-- FORM CONTAINER FIXED -->
    <div class="flex justify-center mt-12 px-4">
      <div class="w-full max-w-3xl glow-form rounded-xl p-10">
        <h1 class="text-4xl font-bold text-white text-center mb-8 drop-shadow-lg">Update Item</h1>
        <form action="/dashboard/edit/<%= item._id %>" method="POST" class="space-y-6">
          <!-- form fields -->
        <div>
          <label for="name" class="block mb-2 text-sm font-semibold">Name:</label>
          <input type="text" id="name" name="name" value="<%= item.name %>" class="glow-input w-full px-4 py-2 rounded-md bg-transparent border border-gray-500 text-white" />
        </div>
        <div>
          <label for="description" class="block mb-2 text-sm font-semibold">Description:</label>
          <textarea id="description" name="description" rows="4" class="glow-input w-full px-4 py-2 rounded-md bg-transparent border border-gray-500 text-white resize-none"><%= item.description %></textarea>
        </div>
        <div>
          <label for="price" class="block mb-2 text-sm font-semibold">Price:</label>
          <input type="text" id="price" name="price" value="<%= item.price %>" class="glow-input w-full px-4 py-2 rounded-md bg-transparent border border-gray-500 text-white" />
        </div>
        <div>
          <label for="quantity" class="block mb-2 text-sm font-semibold">Quantity:</label>
          <input type="number" id="quantity" name="quantity" value="<%= item.quantity %>" min="0" class="glow-input w-full px-4 py-2 rounded-md bg-transparent border border-gray-500 text-white" />
        </div>
        <button type="submit" class="glow-button w-full py-3 text-lg font-bold text-white rounded-md">Save Changes</button>
      </form>
    </div>
  </body>
</html>
