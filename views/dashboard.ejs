<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Dashboard - Elegance Wheels</title>
  <link rel="icon" href="/favicon.png" sizes="32x32" />
  <link rel="icon" href="https://toyota-indus.com/wp-content/uploads/2022/12/Fav-Icon-32x32-1.jpg" sizes="32x32" />
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500&display=swap" rel="stylesheet" />
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body, html {
      height: 100%;
      font-family: 'Orbitron', sans-serif;
      background: #000;
      color: white;
    }
    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 40px;
      background-color: rgba(0, 0, 0, 0.8);
      box-shadow: 0 0 10px #e30613;
      position: sticky;
      top: 0;
      z-index: 1000;
    }
    .navbar .nav-links {
      display: flex;
      gap: 25px;
      list-style: none;
    }
    .navbar .nav-links li a {
      color: #fff;
      text-decoration: none;
      font-weight: bold;
      font-size: 1em;
      padding: 10px 15px;
      border-radius: 20px;
      transition: all 0.3s ease;
    }
    .navbar .nav-links li a:hover {
      background-color: #e30613;
      box-shadow: 0 0 10px #e30613;
    }
    .hero {
      position: relative;
      height: 90vh;
      background: url('dash-bg.jpg') center/cover no-repeat;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .overlay {
      position: absolute;
      top: 0; left: 0; right: 0; bottom: 0;
      background: rgba(0, 0, 0, 0.7);
      z-index: 1;
    }
    .content {
      position: relative;
      z-index: 2;
      text-align: center;
      animation: fadeIn 2s ease-in forwards;
      opacity: 0;
    }
    @keyframes fadeIn {
      to {
        opacity: 1;
      }
    }
    h1 {
      font-size: 3.5em;
      margin-bottom: 20px;
      color: #fff;
      text-shadow: 0 0 10px rgba(255,255,255,0.3);
    }
    .dashboard-button {
      background: #e30613;
      border: none;
      padding: 15px 40px;
      font-size: 1.2em;
      color: white;
      border-radius: 30px;
      cursor: pointer;
      box-shadow: 0 0 10px #e30613, 0 0 20px #e30613, 0 0 30px #e30613;
      animation: pulse 2s infinite;
    }
    .dashboard-button:hover {
      background: #fff;
      color: #e30613;
      box-shadow: 0 0 20px white;
    }
    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.1); }
      100% { transform: scale(1); }
    }
    .admin-sections {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 30px;
      padding: 40px;
      background: #111;
    }
    .admin-sections a {
      text-decoration: none;
      color: inherit;
    }

    .admin-card {
      display: block;
      cursor: pointer;
      background: #222;
      border: 1px solid #e30613;
      border-radius: 15px;
      padding: 30px;
      text-align: center;
      box-shadow: 0 0 15px #e30613;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      animation: slideUp 0.8s ease forwards;
      opacity: 0;
    }

    .admin-card:hover {
      transform: scale(1.05);
      box-shadow: 0 0 25px #e30613;
    }

    @keyframes slideUp {
    0% {
      opacity: 0;
      transform: translateY(20px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }  
    .admin-card {
      background: #222;
      border: 1px solid #e30613;
      border-radius: 15px;
      padding: 30px;
      text-align: center;
      box-shadow: 0 0 15px #e30613;
      transition: transform 0.3s ease;
    }
    .admin-card:hover {
      transform: scale(1.05);
    }
    .admin-card h2 {
      margin-bottom: 15px;
      font-size: 1.5em;
      color: #e30613;
    }
    .admin-card p {
      font-size: 1em;
      color: #ccc;
      cursor: pointer;
    }
    footer {
      background: #000;
      color: #aaa;
      text-align: center;
      padding: 20px;
      font-size: 0.9em;
      border-top: 1px solid #333;
    }
  </style>
</head>
<body>
    <header class="navbar">
        <div class="logo">
            <span class="font-['Pacifico'] text-6xl text-white mb-3 tracking-wide">Elegance Wheels</span>
        </div>
        <nav>
          <ul class="nav-links">
            <li><a href="/dashboard">Home</a></li>
            <li><a href="/dashboard/create">Create</a></li>
            <li><a href="/dashboard/read">Read</a></li>
            <li><a href="/dashboard/orders">Orders</a></li>
            <li><a href="/logout">Logout</a></li>
          </ul>
        </nav>
      </header>      

  <div class="hero">
    <div class="overlay"></div>
    <div class="content">
      <h1>Welcome, Admin</h1>
      <button class="dashboard-button" onclick="location.href='/dashboard/read'">Go to Dashboard</button>
    </div>
  </div>

  <section class="admin-sections">
    <a href="/dashboard/create" class="admin-card">
      <h2>🚗 Create</h2>
      <p>Add new car listings and keep inventory updated.</p>
    </a>
    <a href="/dashboard/read" class="admin-card">
      <h2>📋 View</h2>
      <p>Browse and manage existing listings.</p>
    </a>
    <a href="/dashboard/orders" class="admin-card">
      <h2>📦 Orders</h2>
      <p>Track and manage customer orders and test drive bookings.</p>
    </a>
    <a href="/logout" class="admin-card">
      <h2>🔒 Logout</h2>
      <p>Sign out securely from the dashboard.</p>
    </a>
  </section>    

  <footer>
    © 2024 Toyota Admin Panel. All rights reserved.
  </footer>
</body>
</html>